/**
 * 这是一个测试Class,调用方法如下：
 * ```typescript
 * // We can initialize like this
 * const sdk = new frontendSdk();
 * ```
 */
export default class frontendSdk {
  /**
   * @description:
   * @param {string} id
   * @param {string} url
   * @return {*}
   */
}
